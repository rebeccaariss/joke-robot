// ☝️ Set up local environment variables by copying this file to 'config.js'
// and replacing the string values with your actual API keys.
// This step is necessary unless the application is deployed with the API
// keys configured elsewhere.

const config = {
  jokeApiKey: 'YOUR_JOKEAPI_KEY', // JokeAPI
  ttsApiKey: 'YOUR_TTS_API_KEY' // Text-to-Speech
};

export default config;
